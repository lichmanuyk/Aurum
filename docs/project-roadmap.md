# План долгосрочного форка Aurum

Статус на 2026-09-25. Основан на исходных фазах проекта; перенос конкретных
пользовательских данных не означает, что универсальный механизм миграции завершён.

| Фаза | Статус | Результат / следующий шаг |
| --- | --- | --- |
| 0. Аудит и архитектура | Выполнено | Модель данных и денежные зависимости описаны. |
| 1. Основа мультивалютности | Выполнено | Native currencies, исторические FX, точность, миграции, backup. |
| 2. Мультивалютность во всех разделах | Выполнено | Счета, отчёты, капитал, бюджеты, цели, крипта и предупреждения используют валюты. |
| 3. Начальные остатки и корректировки | Выполнено | Opening/reconciliation/migration не загрязняют доходы и расходы. |
| 4. Миграция Monefy | Основной путь выполнен; расширение частично | Переводы, исходные строки и сверка сохранены. Универсальная классификация целей/инвестиций и категорий в теги остаётся отдельной задачей. |
| 5. Финальный перенос | Выполнен для предоставленного снимка | Остатки и отчёты сверены, криптоостатки перенесены в постоянную базу `aurum-personal`. Регулярные бэкапы настроены, восстановление проверено; автообновление NBP и свежесть крипты реализованы (PR №4 в `personal`). |
| Закрепление качества | P0 выполнен; P1 начат, P2 впереди | Серверные и браузерные тесты проверяют повседневный учёт, переводы, корректировки, связанные операции с активами, бюджет с разделённой покупкой, регулярные платежи и восстановление. [План сквозных проверок](flow-test-plan.md) разбит на P0/P1/P2. |
| Валюта отображения сводок | Выполнено | PR №5 в `personal`: переключатель PLN/USD/EUR в капитале, крипте и месячном дашборде; основная валюта учёта не меняется. |
| 6. Покупка и продажа активов за деньги со счетов | Выполнено (PR №13, рабочая версия) | Связанная операция одновременно меняет счёт и актив, учитывает комиссию и не попадает в доходы/расходы. Для крипты пока доступны только операции сегодняшним днём при наличии котировки; ручной актив допускает одну связанную операцию в день. |
| 7. PR в upstream | Позже, по отдельному решению | Выделить универсальные части из персонального форка. |

## Что делать дальше

Повседневный учёт уже доступен: зарплата, покупки, другие доходы и расходы,
переводы, остатки и отчёты. Рабочая база `aurum-personal`, ежедневные копии
в iCloud Drive с хранением 30 дней и восстановление отдельной копии проверены.
Автообновление NBP/криптокотировок и переключение валюты сводок работают.
Локальное автообновление Docker после новых коммитов `personal` вынесено в
отдельную установку на Mac: оно не зависит от текущей ветки разработки,
сохраняет копию перед пересборкой и проверяет запуск на порту 3003.

1. **Сделано — закреплены повседневные сценарии.** P0 из
   [плана сквозных проверок](flow-test-plan.md) проверяет зарплату, покупку,
   перевод, начальный остаток и восстановление на синтетических данных.
2. **Сделано — деньги и активы связаны.** Покупка/продажа меняет деньги и
   оценку актива одной операцией; повторная отправка, редактирование,
   удаление, комиссии и копии проверены на отдельной базе. Версия на порту
   3003 обновлена, новая резервная копия формата 5 проверена: 9908 операций,
   счета, активы и котировки совпали с копией до миграции.
3. **Текущий шаг — P1.** Бюджет и разделённая покупка, регулярное списание
   и импорт CSV проверены от формы до баланса. Следом проверить криптоостаток и остальные сценарии
   P1 и P2 из плана по частоте использования. При необходимости связать цели с
   резервированием денег; разрешить несколько связанных сделок по одному
   ручному активу за день; восстановить старые криптосделки и себестоимость
   из выгрузок без удвоения текущих количеств; отдельно решить, какие
   универсальные части отправлять в upstream.

Личные операции или дополнительные примеры от пользователя для шагов 1–2
не нужны. Выгрузки сделок понадобятся только для восстановления истории в
шаге 3.

## Валюта отображения капитала и сводок

Пользователь живёт в Польше: PLN удобен для повседневных расходов и налогов,
а общий капитал и крупные суммы понятнее в USD или EUR.

- Сохранить PLN основной валютой учёта. Валюты счетов, суммы операций,
  бюджетов и целей не меняются при переключении отображения.
- На страницах капитала, криптовалют и месячного дашборда добавить выбор
  PLN / USD / EUR для итогов и основных графиков, включая доходы и расходы.
- Рассмотреть дополнительный эквивалент рядом с основной суммой, например
  USD и PLN одновременно. Переключатель или одновременный показ — варианты
  интерфейса, окончательный выбор пока не сделан.
- Сохранять выбранный режим отображения; график, итог, подписи и подсказки
  должны использовать согласованные валюты и явно обозначать их.
- Использовать существующие правила исторических FX для доходов/расходов
  и истории капитала, а для текущей оценки — соответствующие доступные котировки.
  Показывать дату/свежесть курса; отсутствие курса не подменять нулём или 1:1.
- Проверить, что переключение меняет только представление: исходные деньги,
  количество монет и история сохраняются; дополнительный эквивалент не
  суммируется с основной суммой как ещё один актив.

Первый вариант реализован как один переключатель с сохранением выбора в браузере
для всех трёх экранов. Одновременный второй эквивалент остаётся возможным
улучшением после обратной связи пользователя. Финансовые выгрузки не нужны.

## Правило Git

`upstream/main → main → personal → отдельная feature-ветка → PR → personal`.
Каждое исправление регрессии сопровождается проверкой, которая воспроизводит
проблему. Большие независимые улучшения не накапливаются в одной ветке.
